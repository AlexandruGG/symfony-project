{% extends 'base.html.twig' %}

{% block title %}Admin Homepage{% endblock %}


{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello Admin!</h1>

        Select an action below:
        <ul>
            <li>
                <a href="{{ path('create_company') }}">Create New Company</a>
            </li>
            <li>
                <a href="{{ path('add_agent') }}">Add New Agent</a>
            </li>
        </ul>

        <a href="{{ path('app_logout') }}">Log Out</a>
    </div>
{% endblock %}
